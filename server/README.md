# Express + TypeScript server

## Setup

```bash
npm install
```

## Run (dev)

```bash
npm run dev
```

## Build + run (prod)

```bash
npm run build
npm start
```

## Endpoints

- `GET /` -> `OK`
- `GET /health` -> `{ "status": "ok" }`

